# Database migrations

Database migrations are handled via Goose. Please install first if you don't
already have it.

```sh
go install github.com/pressly/goose/v3/cmd/goose@latest
```

Migration files are in sql/schema
